using System;
using System.ComponentModel;
//An abstract class:

// Cannot be instantiated
// Can contain abstract methods(no body)
//Can contain concrete methods (with body)
// Can contain fields, constructors, properties
//Every child class MUST give its own implementation
// Overrides all abstract methods
// No extra methods
//You use the abstract class reference to store child class objects.